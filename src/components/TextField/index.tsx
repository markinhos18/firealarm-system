import * as S from './styles'

const TextField = () => {
  // const placeholderValor = `${props.placeholder}`

  return (
    <S.Wrapper>
      <input
        className="inputLogin"
        // placeholder={placeholderValor}
        // required={props.mandatory}
      />
    </S.Wrapper>
  )
}
export default TextField
